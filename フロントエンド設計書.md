必須ではないです。

**API開発経験を見せたいだけ**なら、FastAPIのSwagger画面だけでも十分です。

```text
http://localhost:8001/docs
```

ただし、**ポートフォリオとして見栄えを良くするならフロントエンドは作った方がよい**です。
採用側が「実際に使えるアプリ」として理解しやすくなります。

# 結論

おすすめ構成はこれです。

```text
Django or FastAPI API
        ↓
Vue.js frontend
        ↓
ブラウザで操作できるAI学習支援アプリ
```

今回なら、Vue.jsで以下の画面を作るとよいです。

```text
1. ログイン画面
2. 生徒一覧画面
3. 生徒詳細画面
4. 授業記録入力画面
5. AIコメント生成画面
6. 宿題提案画面
7. 学習計画生成画面
```

---

# Vue.js設計案

## 技術構成

```text
Frontend:
Vue 3
Vite
TypeScript
Vue Router
Pinia
Axios
Tailwind CSS or Vuetify

Backend:
FastAPI
PostgreSQL
OpenAI API
Docker
```

Vuetifyを使うと管理画面っぽいUIを早く作れます。

---

# 画面構成

## 1. ログイン画面

```text
/login
```

機能：

```text
メールアドレス
パスワード
ログインボタン
JWT保存
```

## 2. ダッシュボード

```text
/dashboard
```

表示内容：

```text
登録生徒数
今月の授業数
未提出課題数
AI生成回数
```

## 3. 生徒一覧

```text
/students
```

機能：

```text
生徒一覧表示
名前検索
学年フィルター
新規登録ボタン
詳細ページ遷移
```

## 4. 生徒詳細

```text
/students/:id
```

表示内容：

```text
基本情報
授業履歴
課題一覧
成績推移
AI生成履歴
```

## 5. 授業記録入力

```text
/lessons/new
```

入力項目：

```text
生徒
科目
授業内容
理解度
講師メモ
宿題
```

ボタン：

```text
保存
AIで保護者向けコメント生成
```

## 6. AIコメント生成

```text
/ai/lesson-feedback
```

FastAPIの以下APIを呼ぶ。

```text
POST /ai/lesson-feedback
```

画面では、授業内容を入力するとAIコメントが生成される。

## 7. 宿題提案

```text
/ai/homework-suggestion
```

FastAPIの以下APIを呼ぶ。

```text
POST /ai/homework-suggestion
```

## 8. 学習計画生成

```text
/ai/study-plan
```

FastAPIの以下APIを呼ぶ。

```text
POST /ai/study-plan
```

---

# ディレクトリ構成

```text
frontend/
├─ src/
│  ├─ main.ts
│  ├─ App.vue
│  ├─ router/
│  │  └─ index.ts
│  ├─ stores/
│  │  └─ authStore.ts
│  ├─ api/
│  │  ├─ client.ts
│  │  ├─ studentsApi.ts
│  │  ├─ lessonsApi.ts
│  │  └─ aiApi.ts
│  ├─ views/
│  │  ├─ LoginView.vue
│  │  ├─ DashboardView.vue
│  │  ├─ StudentListView.vue
│  │  ├─ StudentDetailView.vue
│  │  ├─ LessonFormView.vue
│  │  ├─ AiLessonFeedbackView.vue
│  │  ├─ AiHomeworkSuggestionView.vue
│  │  └─ AiStudyPlanView.vue
│  ├─ components/
│  │  ├─ AppLayout.vue
│  │  ├─ StudentForm.vue
│  │  ├─ LessonForm.vue
│  │  ├─ AiResultCard.vue
│  │  └─ LoadingButton.vue
│  └─ types/
│     ├─ student.ts
│     ├─ lesson.ts
│     └─ ai.ts
├─ Dockerfile
├─ package.json
└─ vite.config.ts
```

---

# APIクライアント設計

## src/api/client.ts

```ts
import axios from "axios";

export const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000,
});

apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem("access_token");

  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }

  return config;
});
```

---

# AI API呼び出し

## src/api/aiApi.ts

```ts
import { apiClient } from "./client";

export type LessonFeedbackRequest = {
  student_name: string;
  subject: string;
  lesson_content: string;
  understanding_level: number;
  teacher_note: string;
};

export type LessonFeedbackResponse = {
  feedback: string;
};

export const generateLessonFeedback = async (
  payload: LessonFeedbackRequest
): Promise<LessonFeedbackResponse> => {
  const response = await apiClient.post<LessonFeedbackResponse>(
    "/ai/lesson-feedback",
    payload
  );

  return response.data;
};
```

---

# Vue画面例

## AiLessonFeedbackView.vue

```vue
<script setup lang="ts">
import { ref } from "vue";
import { generateLessonFeedback } from "../api/aiApi";

const studentName = ref("");
const subject = ref("");
const lessonContent = ref("");
const understandingLevel = ref(3);
const teacherNote = ref("");
const feedback = ref("");
const loading = ref(false);
const errorMessage = ref("");

const handleGenerate = async () => {
  loading.value = true;
  errorMessage.value = "";
  feedback.value = "";

  try {
    const result = await generateLessonFeedback({
      student_name: studentName.value,
      subject: subject.value,
      lesson_content: lessonContent.value,
      understanding_level: understandingLevel.value,
      teacher_note: teacherNote.value,
    });

    feedback.value = result.feedback;
  } catch (error) {
    errorMessage.value = "AIコメント生成に失敗しました。";
  } finally {
    loading.value = false;
  }
};
</script>

<template>
  <main>
    <h1>AI保護者コメント生成</h1>

    <div>
      <label>生徒名</label>
      <input v-model="studentName" />
    </div>

    <div>
      <label>科目</label>
      <input v-model="subject" />
    </div>

    <div>
      <label>授業内容</label>
      <textarea v-model="lessonContent"></textarea>
    </div>

    <div>
      <label>理解度</label>
      <input type="number" min="1" max="5" v-model="understandingLevel" />
    </div>

    <div>
      <label>講師メモ</label>
      <textarea v-model="teacherNote"></textarea>
    </div>

    <button @click="handleGenerate" :disabled="loading">
      {{ loading ? "生成中..." : "AIコメント生成" }}
    </button>

    <p v-if="errorMessage">{{ errorMessage }}</p>

    <section v-if="feedback">
      <h2>生成結果</h2>
      <textarea v-model="feedback"></textarea>
    </section>
  </main>
</template>
```

---

# Docker追加

## docker-compose.yml

```yaml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: tutorhub
      POSTGRES_USER: tutorhub_user
      POSTGRES_PASSWORD: tutorhub_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  ai_service:
    build: ./ai_service
    ports:
      - "8001:8001"
    env_file:
      - .env
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_BASE_URL: http://localhost:8001
    depends_on:
      - ai_service

volumes:
  postgres_data:
```

## frontend/Dockerfile

```dockerfile
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
```

---

# ポートフォリオとしての最小構成

最初から全部作らなくてよいです。

まずはこれだけで十分です。

```text
1. Vue画面
2. FastAPI
3. OpenAI API
4. PostgreSQLログ保存
5. Docker Compose起動
```

最小画面は1つでOKです。

```text
AI保護者コメント生成画面
```

これが動けば、かなり見栄えが良いです。

---

# 採用向けに見せるポイント

READMEにはこう書けます。

```text
Vue.js + FastAPI + PostgreSQL + OpenAI APIを用いたAI学習支援アプリです。
授業記録を入力すると、OpenAI APIを利用して保護者向けコメントを自動生成します。
フロントエンドではVue 3、TypeScript、Axiosを使用し、バックエンドではFastAPIでREST APIを構築しました。
Docker Composeにより、フロントエンド・API・DBをまとめて起動できます。
```

結論として、**フロントエンドは必須ではないが、ポートフォリオならVue.jsで1画面だけでも作る価値が高い**です。
